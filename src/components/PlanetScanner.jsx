// The cinematic descent is now driven by the user's scroll gesture in SpaceScene.
// This component intentionally renders nothing so it cannot reintroduce a HUD card
// or a secondary "Descend" control if it is mounted by a future scene variant.
const PlanetScanner = () => null

export default PlanetScanner
